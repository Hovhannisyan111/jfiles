pipeline {
	agent any
	
	stages {
		stage('Clone repo') {
			steps {
				echo "Checkout repo"
			}
		}

		stage('Install deps') {
			steps {
				echo "Installing deps"
			}
		}
		
		stage('Run test') {
			steps {
				echo 'Running Tests'
			}
		}
	}
